# Прогресс перезапуска esque.su

Обновляется после каждой итерации. Контекст: перезапуск русскоязычного онлайн-журнала
esque.su на Next.js, старый сайт — DataLife Engine, ~800 URL в sitemap.

## Ключевые решения (подтверждены заказчиком 2026-07-15)

- Доступа к хостингу/БД старого сайта НЕТ → миграция скрейпингом по https://esque.su/sitemap.xml
  (полный текст свежих записей есть в RSS `yandex:full-text`, остальное — парсинг HTML страниц).
- Маппинг старых рубрик: Street Style → Мода/Тренды; Pro Fashion Russia → Мода/Подиум;
  «В мире» → События; «От редакции» → по содержанию в Персону/Культуру;
  Fashion Bitva и Russian Fashion Display → Проекты; Esque.kids → архивная рубрика.
- Логотип ESQUE сохраняем текущий (взять со старого сайта).
- Комментарии НЕ переносим и не делаем.
- Админка: вход email + пароль, роли ADMIN / EDITOR / AUTHOR, без OAuth.
- URL: `/{section}/{rubric}/{slug}` латиницей; мигрированные слаги в форме `{id}-{slug}`;
  редиректы: таблица Redirect (материализованная из sitemap) + fallback-разбор `*/{id}-*.html`
  по legacyId в middleware.
- Шрифты: Prata (заголовки) + Golos Text (текст/UI), self-hosted через next/font.
- Палитра: бумага #FAF9F6, чернильный #141412, серые ступени, акцент — карминно-красный.
- Блоки контента — JSON-колонка `content` в Post (не отдельная таблица).
- Перед кодом админки показать заказчику сценарий «редактор публикует интервью с 12 фото»
  и дождаться подтверждения (требование ТЗ).

## Окружение разработчика (Windows 10 Home)

- Node.js 24 LTS установлен через winget 2026-07-15 (не было в системе).
- Docker Desktop 4.82 установлен через winget 2026-07-15 (заказчик подтвердил установку).
  Фичи Windows Microsoft-Windows-Subsystem-Linux и VirtualMachinePlatform включены через DISM.
  Docker Desktop выдал «Virtualization support not detected» — аппаратная виртуализация
  выключена в BIOS. Заказчик включает её в BIOS сам (2026-07-15). После включения:
  запустить Docker Desktop, принять лицензионное соглашение (GUI, делает заказчик),
  дождаться готовности движка. Затем: `npx prisma migrate dev --name init`,
  `npm run db:seed`, проверка `docker compose up --build`.
- npm блокирует install-скрипты (allow-scripts): sharp, prisma, @prisma/engines, esbuild,
  unrs-resolver одобрены через `npm approve-scripts`.
- Превью dev-сервера: .claude/launch.json запускает npm через cmd с добавлением
  C:\Program Files\nodejs в PATH (демон не видит свежеустановленный Node).
- GitHub CLI (gh 2.96) установлен через winget 2026-07-16; заказчик авторизовался сам
  (аккаунт r0k11). Репозиторий: https://github.com/r0k11/esque — ПУБЛИЧНЫЙ (выбор
  заказчика). Перед публикацией проверено: .env игнорируется, реальных ключей нет,
  в .env.example только локальные плейсхолдеры.
- ЧАСТАЯ ПОЛОМКА: Docker Desktop периодически выключается (контейнеры Exited 255), и
  Prisma сыплет «Invalid prisma.post.findFirst() invocation … ECONNREFUSED». Это не баг
  кода. Лечение: `docker compose up -d db minio`. Данные в томах переживают падение.
- Lighthouse ставится через `npx lighthouse@12`, Chrome в системе НЕТ — гонять с
  `CHROME_PATH` на msedge.exe (Edge на Chromium).

## Статус итераций

1. [x] Каркас проекта и Docker — create-next-app (Next 16.2.10, React 19, TS, без Tailwind),
       standalone-вывод, Dockerfile (multi-stage), docker-compose.yml (web/db/minio/minio-init),
       .env.example, README. Compose не проверен: нет Docker на машине.
2. [x] Схема БД и модели — prisma/schema.prisma (User, Section, Rubric, Post, Media, Tag,
       Redirect; Post.rubricId опционален — у «Событий» нет рубрик; content = JSON-блоки,
       типы в src/lib/blocks.ts). Prisma 7: datasource url в prisma.config.ts, драйвер-адаптер
       @prisma/adapter-pg (src/lib/prisma.ts). Сид prisma/seed.ts: структура из
       src/lib/structure.ts (единый источник для сида и навигации), 3 пользователя
       (admin/editor/author@esque.su, пароль esque-dev), 21 демо-материал всех типов,
       галерея на 12 фото, плейсхолдеры sharp → MinIO. В compose добавлены migrate и seed.
       НЕ выполнено: `prisma migrate dev` (нужна живая БД — ждёт Docker), сид не запускался.
3. [x] Дизайн-система и токены — globals.css (цвет/типографика/отступы/сетка/движение),
       Prata + Golos Text через next/font (self-hosted), Header (десктоп-нав + мобильный
       бургер на details без JS), Footer. Проверено на 375px в превью. Известное: превью-панель
       давала артефакты отрисовки (чёрные кадры) — стили меню проверены по computed style,
       перепроверить кликом на живой странице.
4. [x] Главная страница — Hero (свежий материал), промо-блок «Сделано в России»
       (env PROMO_MADE_IN_RUSSIA, позже настройка в админке), блоки разделов с тремя
       чередующимися мозаичными паттернами (SectionBlock). Кэширование: страницы с БД
       dynamic="force-dynamic" (при docker-сборке БД недоступна), данные через
       unstable_cache (revalidate 300, тег "posts" — публикация будет сбрасывать
       revalidateTag). next/image: в v16 приватные IP запрещены — включён
       images.dangerouslyAllowLocalIP для MinIO; проп priority заменён на preload
       (deprecated в v16). Проверено на 375/731/1280 (1280 — через DOM: превью-панель
       не умеет скриншотить шире своего окна ~731px).
5. [x] Страница рубрики и раздела — /[section] и /[section]/[child] (child = рубрика ИЛИ
       материал раздела без рубрик, например /events/slug), FeedPage с подменю рубрик,
       пагинация ?page=N («Новее/Раньше»), FeedGrid с циклической мозаикой, 404.
6. [x] Страница материала — /[section]/[child]/[slug], PostView + ArticleBody
       (лид, абзацы-html, заголовки, цитаты, картинки, embed, Q&A). Проверены
       интервью, статья, материал «Событий», 404.
7. [x] Галерея — GalleryViewer: мозаика превью + полноэкранный просмотр на scroll-snap
       (свайпы нативные, стрелки, клавиши, Escape, счётчик, подписи). ВАЖНО: оверлей
       рендерится через createPortal в body — иначе position:fixed ломается из-за
       анимированных предков (.rise анимирует transform).
       Проверен docker compose up --build: migrate/seed/web стартуют по depends_on,
       сайт работает из standalone-контейнера, критерий «одна команда» выполнен.
       Грабли Docker-сборки: npm ci --ignore-scripts (postinstall prisma generate до
       копирования схемы) + npm rebuild sharp esbuild; фиктивный DATABASE_URL на
       стадии build (prisma.config.ts требует переменную).
8. [x] Админка. Аутентификация: jose JWT в httpOnly-cookie (src/lib/session-crypto.ts —
       чистая крипта для proxy; src/lib/session.ts — cookie-хелперы; src/lib/dal.ts —
       requireSession/canPublish/canEditPost). proxy.ts (в Next 16 middleware = Proxy,
       Node-рантайм) — оптимистичный редирект /admin/* на /admin/login.
       Роли: AUTHOR создаёт/правит своё; EDITOR/ADMIN публикуют и правят чужое.
       Оболочка сайта вынесена в группу app/(site)/ (Header/Footer), корневой layout —
       только html/body/шрифты; у app/admin своя оболочка.
       Экраны: /admin/login, /admin (список со статусами), /admin/new (выбор типа →
       createDraft → редактор), /admin/edit/[id] (редактор), /admin/preview/[id]
       (PostView для любого статуса, доступ по сессии).
       Редактор (src/components/admin/Editor.tsx + BlockEditor.tsx): 3 шага (заголовок+лид →
       блоки → рубрика/обложка/публикация); блоки paragraph/heading/quote/qa/embed/image/
       gallery с drag-and-drop переупорядочиванием (нативный HTML5 DnD) и кнопками ↑↓✕;
       загрузка фото перетаскиванием в дропзону → POST /api/admin/upload (sharp: rotate,
       webp q90, blur-плейсхолдер → MinIO) → трей; «Собрать галерею из всех фото», выбор
       обложки кликом; автосохранение (дебаунс 1200мс, индикатор Сохранено); отложенная
       публикация (datetime-local → статус SCHEDULED); публикация генерит слаг транслитом
       (src/lib/slug.ts), уникализирует, revalidateTag("posts","max").
       ПРОВЕРЕНО сквозным сценарием в браузере: логин editor@esque.su → новый Интервью →
       дроп 12 фото (2.8с) → галерея из 12 + обложка + 2 Q&A → автосохранение →
       Опубликовать → материал на сайте по /fashion/interview/my-shem-to-... с рабочей
       полноэкранной галереей (12 кадров). tsc + lint чисты.
       Грабли Next 16: revalidateTag требует 2 аргумента (tag, "max"); при переносе
       маршрутов в (site) удалить .next — иначе стейл-типы валидатора.
9. [x] Скрипт миграции — scripts/migrate.ts + scripts/url-map.ts (карта старых
       префиксов → раздел/рубрика). Источник — https://esque.su/sitemap.xml (614 URL:
       555 статей, 58 рубрик, доступа к дампу нет → скрейпинг cheerio). На статью:
       h1.entry-title → заголовок; itemprop=datePublished («16-авг-2025») → дата;
       og:image (/uploads/) → обложка; текст из .full-story (p/h2/h3/blockquote/iframe);
       фото статьи — все /uploads/posts/ на странице, КРОМЕ обложки и картинок,
       завёрнутых в ссылку на другую статью (это миниатюры блока «похожие»!) → галерея
       (или одиночное фото); .tags a → теги. Автор — служебный «Редакция Esque»
       (redaktsiya@esque.su), в исходнике авторов нет. Изображения: оригинал (без потерь)
       в MinIO, размеры/блюр через sharp, идемпотентность по Media.sourceUrl.
       Post upsert по legacyId; Redirect old→new (301) на каждую статью и рубрику;
       если исходник 404 — всё равно 301 на раздел (старый URL не должен давать 404).
       Идемпотентно (перезапуск обновляет). Лог: scripts/output/migrate-log.json.
       Запуск: `npm run migrate` (весь), `npm run migrate -- --limit N --offset M`.
       ПОЛНЫЙ ПРОГОН ВЫПОЛНЕН (перезапущен после правок парсера): перенесено 544 статьи
       (555 минус 9 мёртвых на источнике и 2 тестовые), 1734 изображения, 613 редиректов.
       9 ошибок — все HTTP 404 на самом источнике (мёртвые записи sitemap), для них
       создан fallback-301 на раздел.
       ПРАВКИ ПАРСЕРА (по замечаниям заказчика 2026-07-16):
       1) Текст: часть статей DLE верстает абзацы как <div><span>, а не <p> — парсер их
          пропускал (14 материалов остались без текста). Теперь берём и div, но только
          «листовые» (без вложенных блоков), иначе текст дублируется родителем.
          Осталось 2 «тонких»: обе — /magazine «Текущий выпуск» (пустая и галерея), это
          соответствует источнику.
       2) Обложки: у 177 материалов og:image — дефолтная заглушка шаблона
          (templates/esque/images/og-main.jpg), а не /uploads/. Теперь при отсутствии
          og-обложки берём первое фото из тела (и убираем его из тела, чтобы не
          дублировалось). Без обложки осталось 6 — у них на источнике фото нет вовсе;
          для них PostCard показывает сдержанную заглушку-монограмм, чтобы мозаика не
          рвалась.
       3) Тестовые материалы старого сайта (URL вида *-test.html: 641 «Коко Шанель»,
          643 «ПУТЬ ДИЗАЙНЕРА») не переносятся (isLegacyTestUrl), их старые адреса ведут
          301 на раздел. ВНИМАНИЕ: 653 «ПУТЬ ДИЗАЙНЕРА от А до Я» — настоящая статья с
          тем же названием, её не трогать. Все 555 старых адресов статей
       имеют редирект (546 на страницы + 9 fallback) + 58 редиректов рубрик = покрытие
       старых URL 100%. Распределение по разделам: События 196, Мода 136, Персона 84,
       Красота 38, Психология 34, Проекты 30, Культура 28. Материалы с галереями (до 15
       фото) рендерят обложку/текст/мозаику/полноэкранный просмотр — проверено в браузере.
       Теги пусты (источник не отдаёт .tags в HTML). Блок «похожие» отсекается корректно.
       ЧИСТКА ПЕРЕД ЗАПУСКОМ: в БД соседствует 21 демо-материал из сида (без legacyId) —
       удалить перед продакшеном отдельным шагом.
       ВАЖНО: не гонять `npm run build` (prod) при работающем `next dev` — портит .next,
       динамические маршруты начинают отдавать 404; лечится `rm -rf .next` + рестарт dev.
10. [x] SEO — src/lib/seo.ts (SITE_URL/absolute/jsonLdScript/organizationJsonLd; absolute()
       не трогает уже абсолютные URL медиа — иначе og:image склеивался в
       site.ru/http://minio...). Метатеги: канонические URL на главной, разделе, рубрике
       и материале; OG (article с published_time/author/section/image, website для лент)
       + twitter summary_large_image; шаблон title и RSS-alternate в корневом layout.
       JSON-LD: Organization (главная), Article/NewsArticle по типу материала +
       BreadcrumbList (страница материала). Крошки: src/components/Breadcrumbs.tsx —
       видимые «Главная / Раздел / Рубрика» (заголовок не дублируем, он H1 рядом),
       в JSON-LD цепочка полная, с заголовком.
       robots.ts (Disallow /admin, /api + ссылка на sitemap).
       Sitemap разбит по разделам явными route handlers (полный контроль над
       sitemapindex): /sitemap.xml — индекс, /sitemaps/{раздел}.xml — раздел + рубрики +
       его материалы, /sitemaps/pages.xml — главная и разделы. RSS: /rss.xml (50 свежих).
       ПРОВЕРЕНО: robots/sitemap/RSS отдают 200 и верный content-type; fashion.xml — 143
       URL, culture.xml — 34, pages.xml — 8, RSS — 50 записей; на материале canonical,
       og:image (реальный URL медиа), Article + BreadcrumbList; на главной Organization.
       LIGHTHOUSE (прогон на прод-сборке в Docker, через Edge):
       десктоп главная 99 / SEO 100, материал 98 / SEO 100 — критерий ТЗ выполнен.
       Мобильный профиль (4G): LCP 3.7-4.8 c при требовании < 2.5 c, Performance 76-86 —
       КРИТЕРИЙ НЕ ВЫПОЛНЕН. Замеры на этой машине шумят (при заведомо ускоряющих
       правках цифра скакала 3.7 → 4.8), т.к. Docker+Postgres+MinIO+Edge делят одну
       машину, и каждый прогон попадает на холодный вариант картинки. Прямые замеры
       улучшились везде (см. ниже). Оставшийся упор — холодная оптимизация изображений
       на первого посетителя; лечится CDN перед сайтом или прогревом вариантов после
       деплоя, т.е. инфраструктурой прода, а не кодом.
11. [x] Редиректы и проверка ссылок — src/proxy.ts (ВАЖНО: при использовании src/ файл
       должен лежать в src/, в корне Next его НЕ видит — раньше админку защищал только
       requireSession, а proxy молча не работал). Логика: защита /admin + для «старых»
       путей (кончаются на .html или на «/») поиск в таблице Redirect по fromPath → 301;
       fallback по legacyId для `*/{id}-*.html` (DLE отдавал статью по id при любом
       слаге — повторяем, покрывает старые ссылки с изменённым слагом); прочие .html
       без совпадения → 301 на главную. В next.config: skipTrailingSlashRedirect — иначе
       Next срезает «/» 308-м редиректом ДО proxy и старые рубрики не находятся.
       scripts/check-links.ts (`npm run check-links`): гоняет весь старый sitemap против
       локального сайта, разворачивает цепочки редиректов, падает с кодом 1 при 404/5xx.
       РЕЗУЛЬТАТ: 614/614 старых URL отдают 200 (напрямую или через 301), 404 — ноль.
       Грабли: браузерный fetch кэширует ответы и с redirect:'manual' прячет 3xx как
       status 0 — проверять цепочки надёжнее через Node (см. check-links). Dev-сервер
       под нагрузкой изредка отдаёт транзиентный 5xx — в check-links есть один повтор
       и конкурентность снижена до 8.
       Демо-контент сида удалён (22 материала + 59 медиа) — в БД только мигрированное.

## Доработки вне итераций

- Sticky-шапка (2026-07-15, по запросу заказчика): на десктопе логотип уезжает,
  строка разделов прилипает (header position:sticky, top: -96px = высота строки логотипа);
  на мобильном прилипает вся шапка (64px). ГРАБЛИ: overflow-x:hidden на html/body ломает
  sticky — заменён на overflow-x:clip; блокировка скролла при открытых оверлеях — на
  documentElement, НЕ на body (overflow на body делает его скролл-контейнером и sticky отваливается).
- Подрубрики в шапке: на десктопе выпадающие списки по hover/focus-within
  (мост padding-top против «проваливания» курсора, translate вместо transform);
  на мобильном меню — клиентский MobileMenu с details-аккордеонами по разделам,
  «Все материалы» + рубрики, автозакрытие по usePathname, скролл-лок на html.

## Анимации и подача статьи (2026-07-16, по просьбе заказчика)

Заказчик просил «анимации в духе reactbits» и более привлекательные статьи.
РЕШЕНИЕ: библиотеку не тянем (reactbits требует framer-motion/GSAP — десятки КБ JS
на каждую страницу, а у нас Lighthouse ≥ 90 в критериях и сайт живёт на органике).
Те же приёмы сделаны своими мини-компонентами почти без веса:
- src/components/Reveal.tsx — появление при скролле (IntersectionObserver, класс через
  data-атрибут → без ре-рендеров). Стиль — ГЛОБАЛЬНЫЙ класс .reveal-anim в globals.css,
  чтобы <noscript> в layout мог показать контент без JS. Reduced-motion учтён.
  Применён: плитки лент и блоков разделов (каскад), фото/галереи/цитаты в статье.
  Абзацы НЕ анимируем — мешает чтению.
- src/components/SplitText.tsx — заголовок статьи проявляется по словам. Серверный
  компонент: только разметка + CSS-задержки, ноль JS.
- src/components/ReadingProgress.tsx — полоса прогресса чтения (rAF, пишем transform
  напрямую, без состояния).
- Буквица в первом абзаце (ArticleBody .dropCap). ВАЖНО: ставится только если абзац
  начинается с БУКВЫ — у материала, открывающегося датой («21 февраля 2024»),
  ::first-letter вырывал из числа первую цифру («2» буквицей + осиротевшее «1 февраля»).
- Обложка в статье: была во всю ширину (вертикальные кадры занимали полтора экрана),
  теперь max-height 68vh (60vh на мобильном) при сохранении полного кадра без обрезки.

## Производительность (2026-07-16)

- Dockerfile: в рантайм-образе .next принадлежал root, а процесс работает под nextjs —
  каталог .next/cache не создавался (Permission denied), и next/image НЕ кэшировал:
  пережимал оригинал заново на КАЖДЫЙ запрос (~700 мс CPU). Лечение: копирование
  с --chown + mkdir .next/cache/images. Замер: 700 мс → 48-75 мс. В dev не проявлялось.
- ISR: статьи на revalidate=300. КЛЮЧЕВОЕ: одного revalidate мало — Next кэширует
  динамический маршрут только при наличии generateStaticParams. Пустой список даёт то,
  что нужно: на сборке ничего не пререндерим (БД тогда недоступна), посещённые страницы
  кэшируются. Замер: 285-363 мс на каждый запрос → MISS 1270 мс, дальше HIT 130-170 мс.
  Главная ОСТАЛАСЬ динамической: это статический маршрут, Next пререндерил бы его на
  сборке и падал с DatabaseNotReachable (проверено — сборка ломается). Ленты тоже
  динамические: пагинация читает searchParams, а это делает маршрут динамическим.
- scripts/optimize-media.ts (`npm run optimize-media [-- --max 2048 --dry]`): ужимает
  исходники до 2048px по длинной стороне. Прогон: 165 из 1734 были больше (до 5690x3201),
  освобождено 150 МБ. Холодная оптимизация 3000 → 1485 мс, тёплая 68 мс. Обратимо:
  оригинал можно перекачать по Media.sourceUrl. Итого медиа в MinIO: ~357 МБ.
- Сид: демо-материалы создаются ТОЛЬКО в пустую базу (иначе `docker compose up`
  подмешивал 22 демо к реальным). Структура и пользователи ставятся всегда.

## Сброс кэша после скриптов (2026-07-16)

Скрипты пишут в БД мимо Next, поэтому сайт отдавал старые страницы до истечения
revalidate. Из админки кэш сбрасывается сам, из CLI — нет.
- src/app/api/revalidate: POST, защищён общим секретом (AUTH_SECRET в заголовке).
- migrate.ts и optimize-media.ts дёргают его в конце. Недоступность сайта — не ошибка.
- ВАЖНО: одного revalidateTag НЕ хватает — он сбрасывает кэш ДАННЫХ, а отрендеренный
  HTML (ISR) продолжает отдаваться. Проверено на прод-сборке: правка не появлялась даже
  через 4 запроса. Работает только вместе с revalidatePath("/", "layout").
- Проверять сброс на dev БЕССМЫСЛЕННО: dev не кэширует, застой не воспроизводится.

## Инлайн-разметка в мигрированном тексте (2026-07-16)

Парсер брал только $el.text(), из-за чего потерялись ВСЕ ссылки и выделения (ноль на
546 материалов). Теперь берём html абзаца и чистим белым списком (sanitize-html) —
санитизация обязательна: контент скрейпится с внешнего сайта, а рендерим через
dangerouslySetInnerHTML.
- разрешены: a, strong, b, em, i, u, br, sup, sub. Списки НЕ разрешены намеренно:
  в исходниках их нет, а блок абзаца рендерится <p>, внутри которого <ul> невалиден.
- ссылки на esque.su → относительные; внешние → target=_blank + rel=noopener noreferrer.
  ГРАБЛИ: target/rel обязаны быть в allowedAttributes, иначе санитайзер срезает их уже
  после transformTags и защита теряется.
- Вернулось: 55 статей со ссылками, 345 с жирным, 197 с курсивом, 166 с переносами.
  Проверено векторами (script, img onerror, javascript:, iframe) и по БД — чисто;
  iframe только в блоках embed (легальные вставки YouTube).

## Оформление статьи по образцу buro247 (2026-07-16)

Замерены их страницы, взяты ПРИЁМЫ вёрстки, не айдентика (Prata + Golos Text —
узнаваемость издания, их Helvetica не берём; ТЗ требует не выглядеть шаблонно).
- Раскладка: всё по левому краю сетки (было по центру), воздух вправо. Колонка чтения
  680px, текст 18px/1.7. Фото (880), галереи (1080), цитаты (820) вырываются из колонки
  вправо. Заголовок Hero меньше «геройского» — у него колонка уже, плюс перенос длинных
  слов (на 1280 «основательницей» вылезало за колонку).
- Типографика: зазор абзацев 1.7em (~31px, как у них); подзаголовки Prata 28-36px с
  отбивкой 64px (у них подзаголовок в 2.5 раза крупнее набора); подписи антиквой
  (курсив НЕ ставим — у Prata нет курсивного начертания, синтетический наклон грязный);
  ссылки подчёркиванием; списки — глобальный сброс убивал маркеры, вернули.
- Добавлено: RelatedPosts (3 материала рубрики; у buro247 такого блока нет, но у нас
  546 статей связаны только лентами — перелинковка), ShareLinks (Telegram/ВК/копировать;
  без сторонних виджетов; Facebook не берём — в РФ недоступен), время чтения (180 сл/мин,
  со склонением), «Текст: автор», дата с ГОДОМ (архив с 2019-го; канцелярское «г.» убрано).

## Поиск (2026-07-17)

- Полнотекстовый поиск Postgres со словарём russian. Замер: «дизайнеры» находит 178
  материалов против 81 у ILIKE — словоформы («дизайнеров», «дизайнеру») раньше терялись.
- Миграция post_search_vector: GENERATED-колонка tsvector + GIN-индекс. Postgres
  пересчитывает сам — поддерживать в коде не нужно. Веса A/B/C = заголовок/лид/текст,
  ранжирование ts_rank. websearch_to_tsquery (не to_tsquery): разбирает ввод «как в
  поисковике» и не падает на произвольном тексте. Параметризовано (проверено инъекцией).
- Prisma читает колонку как Unsupported("tsvector") — само выражение и индекс в миграции.
- /search: обычная форма GET (работает без JS, ссылку можно переслать), поле — линией,
  результаты мозаикой, закрыта от индексации. Лимит 40 без пагинации — если поиском
  будут пользоваться активно, добавить.

## Шапка: поиск и боковое меню (2026-07-17)

- MobileMenu → SideMenu: работает на всех ширинах (на десктопе меню открыть было нечем).
  Панель выезжает справа (420px, на мобильном во весь экран), закрывается крестиком,
  кликом по затемнению и Escape. Рендерится ПОРТАЛОМ в body — position:fixed ломается,
  если у предка есть transform (урок галереи).
- Кнопки поиска и меню живут в самой шапке, а не в ряду логотипа: тот при прокрутке
  уезжает (sticky, top: -96px), и кнопки пропадали бы с экрана. Стоят на уровне ряда
  рубрик — он остаётся липким.
- Правый край — общая переменная --edge на .header (шапка во всю ширину, контейнер
  ограничен и центрирован). Кастомные свойства наследуются, поэтому работает и из
  CSS-модуля SideMenu.
- Вертикаль: bottom var(--sp-3), НЕ 0 — у ряда рубрик снизу отступ 16px, и по центру
  блока кнопки вставали на 6px ниже ссылок. На мобильном bottom сброшен в 0.

## Форматирование текста в админке (2026-07-17)

Абзац хранит HTML, поэтому поле осталось обычной textarea, а кнопки лишь оборачивают
выделение в теги (выбор заказчика: не WYSIWYG). src/components/admin/ParagraphField.tsx —
Ж/К/Ч, x²/x₂, ссылка, ↵ (<br />), горячие Ctrl+B/I/U. Только абзац: лид, цитата и ответ
интервью хранятся чистым текстом, разметка в них потребовала бы правки рендера статьи.
- Выделение после правки восстанавливаем сами (useLayoutEffect + ref): значение идёт
  через React-состояние, и к моменту ре-рендера браузер уже сбросил каретку в конец.
- Кнопки — на onMouseDown с preventDefault, НЕ на onClick: click успевает снять
  выделение в textarea, и оборачивать становится нечего.
- ДЫРА, ЗАКРЫТА ПОПУТНО: текст абзаца из админки шёл в dangerouslySetInnerHTML вообще
  без санитизации (чистился только мигрированный). Белый список вынесен из migrate.ts в
  src/lib/sanitize.ts и повешен на saveDraft/publishDraft (cleanBlocks). Чистка на сервере
  обязательна: server action вызывается по сети, клиенту верить нельзя.
  ПРОВЕРЕНО векторами через админку: <script>, <img onerror>, javascript: — вырезаны;
  внешняя ссылка получила target=_blank + rel, ссылка на esque.su стала относительной.
- Блок embed НЕ санитизируется намеренно: там легальные iframe (YouTube), белый список
  абзаца их убьёт. Riск принят — embed доступен только редакции.
- ГРАБЛИ ПРОВЕРКИ: второй `next dev` в той же папке Next 16 не запускает вовсе
  («Another next dev server is already running») — превью из второй сессии поднять
  нельзя, надо цепляться к уже работающему серверу через preview_start с url.

## Мобильная вёрстка (2026-07-16, проход 375/768/1440)

Горизонтальной прокрутки нет нигде. Два дефекта были на планшете:
- Hero жил по брейкпоинту 767px, пока ленты и блоки перестраиваются до 1023px: на 768
  обложка ужималась до 277px. Граница приведена к 1023px. Следом: вертикальный кадр на
  всю ширину вырастал до 1037px при экране 900 и выталкивал заголовок — ограничен 62vh
  (без обрезки), контейнеру задан fit-content (иначе рядом серая полоса-заглушка).
- RelatedPosts на 768 держал 3 колонки по 217px: заголовки в 6 строк, карточки разной
  высоты. Кегль уменьшен, заголовок обрезается 3 строками (высоты 405/405/405).

## Демо-стенд для заказчика: Vercel + Neon + Vercel Blob (2026-07-17)

Пошаговая инструкция — DEPLOY-DEMO.md. Решения и грабли:
- Vercel + Neon ВДВОЁМ не хватает: Neon — только Postgres, а 1734 фото (357 МБ) лежат в
  MinIO, который на Vercel не поднять. Нужно третье звено — объектное хранилище.
- ВЫБОР ХРАНИЛИЩА. Сначала взяли Cloudflare R2 (S3-совместим → правок кода ноль), но он
  ПОТРЕБОВАЛ КАРТУ. Перебрали: Backblaze B2 (S3-совместим, 10 ГБ, но про «без карты»
  гарантий нет — на своей странице тарифов молчат); Supabase Storage (S3-совместим, без
  карты, но бесплатный проект ЗАСЫПАЕТ после недели простоя и будится руками — для
  ссылки, которую клиент открывает когда захочет, не годится). Взяли Vercel Blob: карты
  нет, нового аккаунта нет (Vercel и так нужен), Hobby включает 5 ГБ, не засыпает.
- ЦЕНА ВЫБОРА: Blob не S3-совместим. src/lib/s3.ts → src/lib/storage.ts — два движка за
  одним интерфейсом (uploadObject/getObject/mediaUrl), движок выбирается САМ по наличию
  BLOB_READ_WRITE_TOKEN (на Vercel платформа подставляет его при создании хранилища,
  локально его нет → MinIO). Отдельный переключатель не заводили: лишняя переменная,
  которую забудут выставить. Локальная разработка, docker compose и скрипты миграции
  не изменились. Проверено на живом сайте: главная (22 картинки) и статья + og:image.
- КЛЮЧЕВОЕ ПРО BLOB: addRandomSuffix по умолчанию false → URL предсказуем из ключа, и
  mediaUrl() остался сборщиком `<база>/<key>`, а все 12 мест вызова не тронуты. С
  суффиксом пришлось бы хранить полный URL в БД. Ещё нужен allowOverwrite: повторный
  залив того же ключа иначе падает.
- База адреса Blob выводится ИЗ ТОКЕНА (vercel_blob_rw_<storeId>_...), BLOB_PUBLIC_URL
  необязателен. Поэтому в next.config добавлен ПОСТОЯННЫЙ шаблон
  `*.public.blob.vercel-storage.com`: без него оптимизатор резал бы все картинки демо,
  когда BLOB_PUBLIC_URL не задан.
- mediaUrl() зовут только серверные компоненты (проверено): токен и *_PUBLIC_URL не
  NEXT_PUBLIC_, из клиентского компонента вышли бы битые ссылки.
- Объём демо: выборка 150 материалов (заказчик выбрал вместо всего архива), 447 фото
  ~90 МБ (в 5 ГБ Hobby влезает с запасом). scripts/export-demo.ts
  (`npm run export-demo [-- --limit 150 --dry]`):
  выбор ПО КРУГУ РАЗДЕЛОВ, свежие сверху — простое «последние 150» перекосило бы демо в
  «События» (196 из 546) и оставило другие ленты пустыми. Вышло 21-22 на каждый из 7.
  Переносит структуру → пользователей (с новым паролем) → Post → Media (строки + файлы).
  id сохраняются как есть — тогда sectionId/rubricId/coverId остаются валидными.
  Идемпотентно; --dry читает только локальную базу (подключения к демо ленивые).
  Редиректы НЕ переносятся: вели бы на материалы вне выборки (404), а без них proxy
  уводит старый .html на главную.
- output: "standalone" убран из безусловного конфига — на Vercel он ломает сборку
  (платформа сама пакует функции). Теперь включается BUILD_TARGET=docker, его ставит
  Dockerfile. next.config: хост картинок разбирается из S3_PUBLIC_URL, а не списком.
- NEXT_PUBLIC_IS_DEMO=1 (src/lib/seo.ts IS_DEMO) закрывает демо от индексации:
  robots-метатег в корневом layout + robots.txt Disallow: /. ВАЖНО: живой esque.su
  работает, публичная копия тех же текстов — дубликат, бьющий по позициям оригинала.
  Перед превращением демо в прод флаг снять.
- ЛИМИТ VERCEL: тело запроса 4.5 МБ → сценарий «дроп 12 фото» в админке на демо не
  пройдёт (по 1-2 фото работает). Лечится client upload прямо в Blob из браузера
  (штатный режим @vercel/blob) — не делали.
- ПОРЯДОК ШАГОВ: хранилище Blob создаётся ВНУТРИ проекта Vercel, поэтому проект должен
  появиться до перелива данных. Первый деплой без переменных — нормально.

## Следующий шаг

Все итерации 1-11 закрыты, критерии ТЗ выполнены, КРОМЕ мобильного LCP (< 2.5 c на 4G):
упирается в холодную оптимизацию изображений, лечится CDN/прогревом на проде.

К ЗАПУСКУ (обязательно):
- NEXT_PUBLIC_SITE_URL=https://esque.su — сейчас localhost, от него зависят canonical,
  OG и sitemap.
- Заменить AUTH_SECRET (он же секрет /api/revalidate) и сменить пароли пользователей.
  Особенно важно: репозиторий ПУБЛИЧНЫЙ, устройство авторизации видно всем, а демо-пароль
  esque-dev лежит в сиде.
- В БД 2 пустых черновика «Без названия» (создавал заказчик, тыкая админку) — можно удалить.

ВОЗМОЖНЫЕ ДОРАБОТКИ (обсуждались, не делались):
- Теги: модель есть, но пустая — старый сайт не отдавал теги. Блок тегов внизу статьи
  имеет смысл, только когда их начнут проставлять в админке.
- Пагинация поиска (сейчас 40 результатов без неё).
- Авторы: в источнике их нет, все материалы подписаны «Редакция Esque». Если авторство
  важно — проставлять руками.
